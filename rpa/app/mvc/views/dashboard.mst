{{>header}}

<h1> {{title}} </h1>

<h4> {{flash.message}} </h4>

<p> {{message}} </p>

{{#session}}
  <p> You're    {{session.user.username}} </p>
  <p> Email     {{session.user.email}} </p>
  <p> id        {{session.user.id}} </p>
  <b> sessionID {{session.id}}</b>
{{/session}}

{{>footer}}
